.container
  %header
    %h1 Momentos

  %section#moments
    - if moments.any?
      %ul.list-moments
        = render moments
    - else
      %p.lead.text-muted.text-center
        Oops... no hay momentos para mostrar
