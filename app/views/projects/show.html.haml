- provide :jumbotron do
  = render 'users/scope'
  %h1= @project.title

- provide :sidebar, render('projects/sidebar/show')
- provide :banner_url, @project.image_url

- if @project.description.present?
  %section.content= markdown(@project.description)
- else
  %p.lead.text-center.text-muted No hay una descripción disponible
