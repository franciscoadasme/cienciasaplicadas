en:
  activerecord:
    models:
      abstract: Abstract
    attributes:
      abstract:
        title: title
        document: document
    errors:
      models:
        abstract:
          attributes:
            document:
              less_than: The file must be less than %{count}
              spoofed_media_type: The file must be either a PDF or Microsoft Word document
            document_content_type:
              invalid: The file must be either a PDF or Microsoft Word document
    hints:
      abstract:
        document: The file must be either a PDF or Microsoft Word document
